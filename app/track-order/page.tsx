'use client';import {useState} from 'react';import Link from 'next/link';export default function Track(){const[id,setId]=useState('');const[show,setShow]=useState(false);return <main className="section container"><Link href="/" className="muted">← Home</Link><span className="eyebrow" style={{display:'block',marginTop:35}}>Stay in the loop</span><h1 style={{fontSize:58,color:'var(--primary)'}}>Track your order.</h1><p className="muted">Enter the demo order number from your confirmation.</p><form className="actions" onSubmit={e=>{e.preventDefault();setShow(true)}}><input className="input" required value={id} onChange={e=>setId(e.target.value)} placeholder="e.g. TS-2048"/><button className="btn">Track</button></form>{show&&<div className="card" style={{maxWidth:650,marginTop:28}}><span className="tag">Order {id}</span><h2>On its way to the rider</h2><p className="muted">Kitchen accepted → Preparing → Rider pickup → Delivered</p><div style={{height:8,background:'var(--muted)',marginTop:12}}><div style={{height:8,width:'66%',background:'var(--mustard)'}}/></div></div>}</main>}
