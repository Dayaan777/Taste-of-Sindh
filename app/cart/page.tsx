'use client';import Link from 'next/link';import {useCart} from '@/components/providers';export default function Cart(){const{lines,change,remove,total}=useCart();return <><header className="nav"><div className="container navin"><Link href="/" className="brand"><span className="brandmark">T</span>Taste of Sindh</Link><Link href="/restaurants" className="muted">Continue browsing</Link></div></header><main className="section"><div className="container"><span className="eyebrow">Your order</span><h1 style={{fontSize:58,color:'var(--primary)'}}>Cart</h1>{!lines.length?<div className="empty"><h2>Your cart is waiting.</h2><p className="muted">Add something delicious from a local kitchen.</p><Link href="/restaurants" className="btn">Browse restaurants</Link></div>:<div style={{maxWidth:760}}>{lines.map(l=><div className="cartline" key={l.id}><div><h3>{l.name}</h3><span className="price">Rs. {l.price.toLocaleString()}</span></div><div className="qty"><button onClick={()=>change(l.id,l.quantity-1)}>−</button><span>{l.quantity}</span><button onClick={()=>change(l.id,l.quantity+1)}>+</button><button onClick={()=>remove(l.id)}>×</button></div></div>)}<div className="total"><span>Total</span><span>Rs. {total.toLocaleString()}</span></div><Link href="/checkout" className="btn">Proceed to checkout</Link></div>}</div></main></>}
