import Link from 'next/link';import {restaurants} from '@/lib/data';export default function Picks(){return <main className="section container"><Link href="/" className="muted">← Home</Link><span className="eyebrow" style={{display:'block',marginTop:35}}>The good stuff</span><h1 style={{fontSize:58,color:'var(--primary)'}}>Top picks.</h1><div className="grid">{restaurants.slice().sort((a,b)=>b.rating-a.rating).slice(0,8).map(r=><Link href={`/restaurants/${r.slug}`} className="card" key={r.slug}><span className="tag">Editor&apos;s pick</span><h3>{r.name}</h3><p className="muted">{r.description}</p><div className="rating">★ {r.rating.toFixed(1)} · {r.delivery}</div></Link>)}</div></main>}
