'use client'
import Link from 'next/link'
import { restaurants, categories } from '@/lib/data'
import { SiteHeader } from '@/components/site-header'
export default function Home() { return <><SiteHeader /><main><section className="hero"><div className="shell"><span className="eyebrow">Hyderabad, Sindh · Since 2024</span><h1>Good food.<br /><em>Local soul.</em></h1><p>From biryani that knows its spices to late-night shawarma runs, find the flavours that make Sindh feel like home.</p><div className="actions"><Link href="/restaurants" className="primary-button">Explore restaurants</Link><Link href="/top-picks" className="secondary-button">See top picks</Link></div></div></section><section className="section"><div className="shell"><div className="section-head"><div><span className="eyebrow">Curated for you</span><h2>What are you craving?</h2></div><Link href="/restaurants" className="muted">View all →</Link></div><div className="filters">{categories.slice(1).map(category => <Link className="tag" key={category} href={`/restaurants?category=${encodeURIComponent(category)}`}>{category}</Link>)}</div><div className="grid">{restaurants.slice(0, 4).map(restaurant => <Link href={`/restaurants/${restaurant.slug}`} className="card" key={restaurant.slug}><div><span className="tag">{restaurant.category}</span><h3>{restaurant.name}</h3><p className="muted">{restaurant.description}</p></div><div className="rating">★ {restaurant.rating.toFixed(1)} · {restaurant.delivery}</div></Link>)}</div></div></section><section className="section" style={{ background: '#000000' }}><div className="shell section-head"><div><span className="eyebrow" style={{ color: 'var(--mustard)' }}>The Sindh edit</span><h2 style={{ color: 'var(--ivory)' }}>Flavours worth<br />crossing town for.</h2></div><Link href="/restaurants" className="primary-button" style={{ background: 'var(--mustard)', color: 'var(--ink)' }}>Start browsing</Link></div></section></main><footer className="footer"><div className="shell">© 2024 Taste of Sindh · Made for hungry Hyderabad</div></footer></> }
