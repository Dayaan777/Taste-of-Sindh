'use client'
import Link from 'next/link'
import { signIn } from 'next-auth/react'
import { useEffect, useState } from 'react'
import { SiteHeader } from '@/components/site-header'
export default function AuthPage() { const [mode, setMode] = useState<'login'|'signup'>('login'); const [message, setMessage] = useState(''); const [googleConfigured, setGoogleConfigured] = useState(false); useEffect(() => { fetch('/api/auth/status').then(response => response.json()).then(data => setGoogleConfigured(data.configured)).catch(() => setGoogleConfigured(false)) } , [])
 return <><SiteHeader /><main className="auth-page shell"><div className="auth-copy"><span className="eyebrow">Your table, remembered</span><h1>Come hungry.<br /><em>Stay awhile.</em></h1><p>Sign in to keep your favourite kitchens close and check out faster.</p></div><section className="auth-card"><div className="segmented"><button className={mode === 'login' ? 'active' : ''} onClick={() => setMode('login')}>Sign in</button><button className={mode === 'signup' ? 'active' : ''} onClick={() => setMode('signup')}>Create account</button></div><button className="google-button" onClick={() => { if (!googleConfigured) setMessage('Google sign-in needs GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, and NEXTAUTH_SECRET configured in your environment.'); else signIn('google') }}>Continue with Google</button><div className="or"><span>or continue with email</span></div><form className="stack" onSubmit={e => { e.preventDefault(); setMessage('Demo email access is ready for this portfolio preview.') }}><label>Email address<input type="email" required placeholder="you@example.com" /></label><label>Password<input type="password" required minLength={6} placeholder="At least 6 characters" /></label><button className="primary-button" type="submit">{mode === 'login' ? 'Sign in' : 'Create account'}</button></form>{message && <p className="form-note" role="status">{message}</p>}<p className="fine-print">By continuing, you agree to our <Link href="/contact">house rules</Link>.</p></section></main></> }
