'use client';import Link from 'next/link';import {useCart} from '@/components/providers';import {useRouter} from 'next/navigation';import {useState} from 'react';export default function Checkout(){const{lines,total,clear}=useCart();const router=useRouter();const[name,setName]=useState('');const[address,setAddress]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();if(name&&address&&lines.length){clear();router.push('/success')}};return <main className="section container"><Link href="/cart" className="muted">← Back to cart</Link><span className="eyebrow" style={{display:'block',marginTop:35}}>Almost there</span><h1 style={{fontSize:58,color:'var(--primary)'}}>Checkout</h1><form className="form" onSubmit={submit}><label className="label">Full name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Your name"/></label><label className="label">Delivery address<textarea required value={address} onChange={e=>setAddress(e.target.value)} placeholder="Street, area, Hyderabad"/></label><div className="total"><span>Order total</span><span>Rs. {total.toLocaleString()}</span></div><button className="btn" type="submit">Place mock order</button></form></main>}
